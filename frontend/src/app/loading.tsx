import { OhsLoadingIndicator } from "@/components/ui/ohs-loading-indicator";

export default function RootLoading() {
  return <OhsLoadingIndicator message="RiskNova yukleniyor, ISG veri akisi hazirlaniyor..." />;
}
